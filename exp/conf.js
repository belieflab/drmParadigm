//******************************************//
//   DRM PARADIGM                           //
//******************************************//

workerId = getParamFromURL("workerId");
// workerId = Math.round(Math.random() * 10000, 2);
const durationForListWords = 150;
const durationForTargetWords = 3000;
const fixationDuration = 1000;
